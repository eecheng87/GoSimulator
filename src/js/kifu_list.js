/*let ul = document.querySelector('.kifu_list');

module.exports = {
    append_item: function(file) {
        let li = window.document.createElement('li');
        li.appendChild(document.createTextNode(file));
        ul.appendChild(li);
    }
};*/